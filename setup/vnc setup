# insert options
sudo gedit /etc/modprobe.d/bcmdhd.conf

>> options bcmdhd op_mode=2

# edit mode?? 기억이 잘 안남..
sudo gedit /etc/NetworkManger/????

# nvidia tx2
ifconfig -> inet address check
10.42.0.1


setting hotspot in xytron pdf
#wifi name : olaf
#wifi pw : nvidia123

# remote PC
turn on REMMINA
새로만들기

서버 : 10.42.0.1
비밀번호 : nvidia
색깊이 : 256색상 

# 해상도 조절
xrandr --fb 1280x720
